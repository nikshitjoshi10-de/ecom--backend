const ROLES = {USER: 'user', SELLER:'seller', ADMIN: 'admin'};
const ROLE_LIST = Object.values(roles);

const STGNUP_ROLES = [ROLES.USER, ROLES.SELLER];

module.exports={roles,ROLE_LIST,STGNUP_ROLES};